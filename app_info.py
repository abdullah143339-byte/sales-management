"""Central application metadata.

Bump APP_VERSION before every release; the publish script does this
automatically and the in-app updater uses it to decide when a new
version is available.
"""

APP_NAME = "Sales Management System"
APP_VERSION = "1.0.0"
APP_ID = "com.salesmanagement.app"
