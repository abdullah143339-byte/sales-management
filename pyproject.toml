[project]
name = "sales-management"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = [
    "Flask>=3.0",
    "psycopg2-binary>=2.9",
    "reportlab>=4.0",
]

[tool.vercel]
entrypoint = "web.app:app"
